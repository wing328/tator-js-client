/**
 * Tator REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import AttributeValue from './AttributeValue';
import FloatArrayQuery from './FloatArrayQuery';

/**
 * The MediaBulkUpdate model module.
 * @module model/MediaBulkUpdate
 * @version 0.0.5
 */
class MediaBulkUpdate {
    /**
     * Constructs a new <code>MediaBulkUpdate</code>.
     * @alias module:model/MediaBulkUpdate
     */
    constructor() { 
        
        MediaBulkUpdate.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>MediaBulkUpdate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/MediaBulkUpdate} obj Optional instance to populate.
     * @return {module:model/MediaBulkUpdate} The populated <code>MediaBulkUpdate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MediaBulkUpdate();

            if (data.hasOwnProperty('attributes')) {
                obj['attributes'] = ApiClient.convertToType(data['attributes'], {'String': AttributeValue});
            }
            if (data.hasOwnProperty('archive_state')) {
                obj['archive_state'] = ApiClient.convertToType(data['archive_state'], 'String');
            }
            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], ['Number']);
            }
            if (data.hasOwnProperty('localization_ids')) {
                obj['localization_ids'] = ApiClient.convertToType(data['localization_ids'], ['Number']);
            }
            if (data.hasOwnProperty('state_ids')) {
                obj['state_ids'] = ApiClient.convertToType(data['state_ids'], ['Number']);
            }
            if (data.hasOwnProperty('float_array')) {
                obj['float_array'] = ApiClient.convertToType(data['float_array'], [FloatArrayQuery]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MediaBulkUpdate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MediaBulkUpdate</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['archive_state'] && !(typeof data['archive_state'] === 'string' || data['archive_state'] instanceof String)) {
            throw new Error("Expected the field `archive_state` to be a primitive type in the JSON string but got " + data['archive_state']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['ids'])) {
            throw new Error("Expected the field `ids` to be an array in the JSON data but got " + data['ids']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['localization_ids'])) {
            throw new Error("Expected the field `localization_ids` to be an array in the JSON data but got " + data['localization_ids']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['state_ids'])) {
            throw new Error("Expected the field `state_ids` to be an array in the JSON data but got " + data['state_ids']);
        }
        if (data['float_array']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['float_array'])) {
                throw new Error("Expected the field `float_array` to be an array in the JSON data but got " + data['float_array']);
            }
            // validate the optional field `float_array` (array)
            for (const item of data['float_array']) {
                FloatArrayQuery.validateJsonObject(item);
            };
        }

        return true;
    }


}



/**
 * Attribute values to bulk update an entity list.
 * @member {Object.<String, module:model/AttributeValue>} attributes
 */
MediaBulkUpdate.prototype['attributes'] = undefined;

/**
 * Marks media for archival or retrieval. Media may not be set directly to `live` or `archived`, the system performs that transition for the user.
 * @member {module:model/MediaBulkUpdate.ArchiveStateEnum} archive_state
 */
MediaBulkUpdate.prototype['archive_state'] = undefined;

/**
 * Array of media IDs to retrieve.
 * @member {Array.<Number>} ids
 */
MediaBulkUpdate.prototype['ids'] = undefined;

/**
 * Array of child localization IDs for which media should be retrieved.
 * @member {Array.<Number>} localization_ids
 */
MediaBulkUpdate.prototype['localization_ids'] = undefined;

/**
 * Array of child state IDs for which media should be retrieved.
 * @member {Array.<Number>} state_ids
 */
MediaBulkUpdate.prototype['state_ids'] = undefined;

/**
 * Searches on `float_array` attributes.
 * @member {Array.<module:model/FloatArrayQuery>} float_array
 */
MediaBulkUpdate.prototype['float_array'] = undefined;





/**
 * Allowed values for the <code>archive_state</code> property.
 * @enum {String}
 * @readonly
 */
MediaBulkUpdate['ArchiveStateEnum'] = {

    /**
     * value: "to_archive"
     * @const
     */
    "archive": "to_archive",

    /**
     * value: "to_live"
     * @const
     */
    "live": "to_live"
};



export default MediaBulkUpdate;

