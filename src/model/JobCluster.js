/**
 * Tator REST API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The JobCluster model module.
 * @module model/JobCluster
 * @version 0.0.5
 */
class JobCluster {
    /**
     * Constructs a new <code>JobCluster</code>.
     * @alias module:model/JobCluster
     */
    constructor() { 
        
        JobCluster.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>JobCluster</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JobCluster} obj Optional instance to populate.
     * @return {module:model/JobCluster} The populated <code>JobCluster</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JobCluster();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('organization')) {
                obj['organization'] = ApiClient.convertToType(data['organization'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('host')) {
                obj['host'] = ApiClient.convertToType(data['host'], 'String');
            }
            if (data.hasOwnProperty('port')) {
                obj['port'] = ApiClient.convertToType(data['port'], 'Number');
            }
            if (data.hasOwnProperty('token')) {
                obj['token'] = ApiClient.convertToType(data['token'], 'String');
            }
            if (data.hasOwnProperty('cert')) {
                obj['cert'] = ApiClient.convertToType(data['cert'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JobCluster</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JobCluster</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['host'] && !(typeof data['host'] === 'string' || data['host'] instanceof String)) {
            throw new Error("Expected the field `host` to be a primitive type in the JSON string but got " + data['host']);
        }
        // ensure the json data is a string
        if (data['token'] && !(typeof data['token'] === 'string' || data['token'] instanceof String)) {
            throw new Error("Expected the field `token` to be a primitive type in the JSON string but got " + data['token']);
        }
        // ensure the json data is a string
        if (data['cert'] && !(typeof data['cert'] === 'string' || data['cert'] instanceof String)) {
            throw new Error("Expected the field `cert` to be a primitive type in the JSON string but got " + data['cert']);
        }

        return true;
    }


}



/**
 * Unique integer identifying the job cluster.
 * @member {Number} id
 */
JobCluster.prototype['id'] = undefined;

/**
 * Unique integer identifying the organization associated with the job cluster.
 * @member {Number} organization
 */
JobCluster.prototype['organization'] = undefined;

/**
 * Unique name of the job cluster.
 * @member {String} name
 */
JobCluster.prototype['name'] = undefined;

/**
 * Hostname where the cluster can be accessed.
 * @member {String} host
 */
JobCluster.prototype['host'] = undefined;

/**
 * Port where the cluster can be accessed.
 * @member {Number} port
 */
JobCluster.prototype['port'] = undefined;

/**
 * Token for accessing the job cluster.
 * @member {String} token
 */
JobCluster.prototype['token'] = undefined;

/**
 * Certificate for accessing the job cluster.
 * @member {String} cert
 */
JobCluster.prototype['cert'] = undefined;






export default JobCluster;

